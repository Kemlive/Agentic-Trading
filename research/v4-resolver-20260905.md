# Uniswap v4 PoolKey Resolver — Robinhood 4663 (2026-09-05)

- PoolManager `0x8366…0951`, V4Quoter `0x8Dc1…F94` (Uniswap registry). Official V4Quoter ABI
  `quoteExactInputSingle` locked from Uniswap v4-periphery source.
- Resolver: `scripts/v4resolve.cjs` — candidate PoolKeys (fees 100–20000, tickSpacings 1–1000,
  both currency orders, hooks = 0) → poolId = keccak256(abi.encode(key)) →
  `PoolManager.getSlot0(poolId)` must return a real sqrtPrice (existence PROVEN on-chain), then quote
  via V4Quoter.
- Result on RH shortlist: **zero initialized vanilla (hooks=0) v4 pools.**
- Gecko-listed SPCX/USDG v4 pool `0xcb6f…` **reverts** on `PoolManager.getSlot0` → does not exist
  under the canonical RH PoolManager.
- Conclusion: shortlist v4 liquidity (if any) is hook/dynamic-fee only (Pons/Twofold/bankr) or an
  index artifact. Testing those requires their hook addresses. **Nothing PASSed, nothing funded.**
